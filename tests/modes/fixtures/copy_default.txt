copy_default.txt [mode: copy]

    Install condition:       always install
    Target device:           /dev/sda [filesystem: ext4]
    Format device:           no
    Target path:             /usr/bin
